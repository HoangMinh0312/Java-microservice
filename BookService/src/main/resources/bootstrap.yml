spring:  
  application:
    name: account
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configServer
#      name: account
      profile: production
#      label: develop
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

  